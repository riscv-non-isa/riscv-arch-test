# Parameter mappings

parameter_definitions:
  - name: STVEC_MODES
    coverpoint: ["TODO"]
    effect:
      [
        "If direct or vectored are not supported, tests relying on those modes will not operate TODO should not be run",
      ]
  - name: STVEC_BASE_ALIGNMENT_VECTORED
    coverpoint: ["all"]
    effect: ["Trap handler is aligned to the appropriate boundary"]
