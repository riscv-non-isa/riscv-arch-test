

=== Zi*  Additional Unpriv Extensions

This section includes a variety of unprivileged extensions.

==== Zicsr Control/Status Register Instructions

include::{testplansdir}/Zicsr.adoc[]

include::norm/Zicsr_norm_rules.adoc[]

[NOTE]
===
TRAP_ON_UNIMPLEMENTED_CSR affects the results of Ssstrict/cp_csrr (<<t-SsstrictS-coverpoints>>).  It does not affect Zicsr because this is an unprivileged test that should never perform operations that trap.
===

==== Zifencei Instruction-Fetch Fence

include::{testplansdir}/Zifencei.adoc[]

include::norm/Zifencei_norm_rules.adoc[]

[[t-Zifencei-coverpoint-definitions]]
.Zifencei Coverpoint Definitions
[cols="2, 1, 4" options=header]
|===
|Coverpoint|Bins|Definition
|cp_custom_fencei|4|Executes normal `fence.i` and `fence.i` instructions with nonzero rs1, rd, and funct12 fields to verify that they execute without trapping.
|===

The Zifencei tests must write an instruction to an address, execute `fence.i`, and then execute the instruction to check that the new instruction is executed.



==== Misalign: Misaligned Loads/Stores

Misaligned loads/stores to main memory optionally can be handled in hardware.  When the UDB MISALIGNED_LDST parameter is true, misaligned scalar loads and stores to main memory do not throw an exception.  This parameter is a simplification of the "Access Type PMAs" which could define misaligned behavior on each memory region.

The misaligned family of tests exercises integer, compressed, and single/double/quad-precision floating-point loads/stores.  It uses the new cp_misalign coverpoint defined in <<t-Misaligned-modified-coverpoints>>. It assumes that if these work, then Zfh and Zcb loads/stores will also work correctly.  Misaligned atomics are exercised with {ExceptionsZaamo, ExceptionsZalrsc}/{cp_amo/load/store_address_misaligned}. Misaligned vector load/stores are exercised in <<MisalignV>>.

include::{testplansdir}/Misalign.adoc[]

include::norm/Misalign_norm_rules.adoc[]

include::{testplansdir}/MisalignF.adoc[]

include::norm/MisalignF_norm_rules.adoc[]

include::{testplansdir}/MisalignD.adoc[]

include::norm/MisalignD_norm_rules.adoc[]

//include::{testplansdir}/MisalignQ.adoc[]

include::norm/MisalignQ_norm_rules.adoc[]

include::{testplansdir}/MisalignZca.adoc[]

include::norm/MisalignZca_norm_rules.adoc[]

[[t-Misalign-coverpoint-definitions]]
.Misalign Coverpoint Definitions
[cols="2, 1, 4" options=header]
|===
|Modified Coverpoint|Bins|Definition
|cp_misalign|8|All 8 combinations of the three least significant bits of the address.
|===

==== Zicclsm Misaligned Loads/Stores

The Zicclsm extension, defined in the https://drive.google.com/file/d/1Kg7Ner5ZlxFDclf92-9Tz88JvmZWt5Wb/view[RVA22] and
https://github.com/riscv/riscv-profiles/blob/main/src/rva23-profile.adoc[RVA23] profiles, provides the following normative rule.  The extension applies to privilege modes below M-mode (because the profiles apply to supervisor-mode execution environment), and requires that misaligned scalar and vector loads and stores to main memory either are handled in hardware or raise a misaligned exception that is handled by the execution environment.  This is tested by Exceptions coverpoints that either return the expected value or throw a misaligned trap. Misaligned atomics are not part of Zicclsm, but are checked in {ExceptionsZaamo, ExceptionsZalrsc}/{cp_load_address_misaligned, cp_store_address_misaligned}.

[[t-Zicclsm-normative-rules]]
.Zicclsm Normative Rules
[cols="1,4,3", options="header"]
|===
|Normative Rule |Rule Text |Coverpoints

|zicclsm_op |Misaligned loads and stores to main memory regions with both the cacheability and coherence PMAs must be supported. |{ExceptionsS, ExceptionsU, ExceptionsZc, ExceptionsF, ExceptionsVls}/{cp_load_address_misaligned, cp_store_address_misaligned}
|===



*** ExceptionsVls coverpoints need to be written.
*** update normative rule when tagged in profile and hyperlink is available


==== Zicond Conditional Instructions

include::{testplansdir}/Zicond.adoc[]

include::norm/Zicond_norm_rules.adoc[]



==== Zicntr Counters

Zicntr provides unprivileged read access to the `cycle`, `time`, and `instret` counters.
Note that, like all other unprivileged tests, these tests are performed in M-mode.
The <<t-ZicntrS-coverpoints, ZicntrS>> and <<t-ZicntrU-coverpoints, ZicntrU>> privileged tests exercise reading the counters in S-mode and U-mode,
with control by `mcounteren` and `scounteren` CSRs.
The <<t-Sm-coverpoints, Sm>> privileged tests exercise `mcycle`, `minstret`, and HPMs from M-mode.

include::{testplansdir}/Zicntr.adoc[]

include::norm/Zicntr_norm_rules.adoc[]

TODO: add normative rule for TIME_CSR_IMPLEMENTED UDB parameter
referencing the following text from the priv spec:

````
emulate this
functionality on behalf of less-privileged modes in M-mode software.
````
This rule is checked by the time bin of cp_cntr coverpoint.


[[t-Zicntr-coverpoint-definition]]
.Zicntr Coverpoint Definition
[cols="2, 1, 4" options=header]
|===
|Coverpoint|Bins|Definition
|cp_cntr|3|Read each of the counters (`cycle`, `time`, `instret`) twice using `csrrs rd, csr, x0` and check that the difference is exactly right for `instret`, or at least 1 for `cycle`, 0 for `time` (`time` may not increment between reads).
For RV32, also check `cycleh`, `timeh`, `instreth` with the same definitions.
Skip `time` and `timeh` if parameter TIME_CSR_IMPLEMENTED = False.
|===

==== Zihpm Hardware Performance Monitors

Zihpm provides unprivileged read access to custom hardware performance monitor CSRs.

include::{testplansdir}/Zihpm.adoc[]

include::norm/Zihpm_norm_rules.adoc[]

[[t-Zihpm-modified-coverpoints]]
.Zihpm Coverpoint Definition
[cols="2, 1, 4" options=header]
|===
|Coverpoint|Bins|Definition
|cp_cntr_hpm|29|Read each of the HPMs twice and check that the difference is non-negative.  For RV32, also check the high halves of each HPM.
|===

Note that this coverpoint assumes that reading hpmcounter* never traps.
See https://github.com/riscv/riscv-isa-manual/issues/2558[RISC-V ISA Manual Issue #2558] for discussion.
There may not be a graceful way to certify Zihpm on an implementation that allows hpmcounter* reads to
sometimes trap because unprivileged tests should not trap.

[NOTE]
===
MCOUNTENABLE_EN, SCOUNTENABLE_EN, HCOUNTENABLE_EN parameters are exercised by ZicsrS/U/H cp_xcounteren_access_y tests (<<t-ZicntrS-coverpoints>>). COUNTINHIBIT_EN is exercised by Sm/cp_inhibit tests (<<t-Sm-coverpoints>>).
===

==== Zihintpause Pause Hint

include::{testplansdir}/Zihintpause.adoc[]

include::norm/Zihintpause_norm_rules.adoc[]



==== Zihintntl Non-temporal Locality Hint

These instructions are hints that behave as nops.  They are simply executed once.

include::{testplansdir}/Zihintntl.adoc[]

include::norm/Zihintntl_norm_rules.adoc[]

==== ZihintntlZca Compressed Non-temporal Locality Hint

Compressed Zihintntl instructions `c.ntl.p1`, `c.ntl.pall`, `c.ntl.s1`, and `c.ntl.all` are a subset of the `c.add` hints.

include::{testplansdir}/ZihintntlZca.adoc[]

include::norm/ZihintntlZca_norm_rules.adoc[]

==== Zicboz Cache Block Zero

The `cbo.zero` instruction zeros a cache line.  The cache line size may vary from one machine to another.

[[t-Zicbo-coverpoint-definition]]
.Zicbo Coverpoint Definition
[cols="2, 1, 4" options=header]
|===
|Coverpoint|Bins|Definition
|cp_custom_cbo|65*2|Writes 65 words (260 bytes) of arbitrary data to scratch memory, executes `cbo` instruction at the aligned start of scratch, and then reads back all 260 bytes and stores them to the signature.  The first cache line (often 64 bytes) of data should be zeroed and the rest unchanged.  It repeats this operation with an offset of 255 bytes.  The instruction ignores the offset within a cache line, so for 64-byte lines, this would zero bytes 192-255 and leave the others unchanged.
|===

include::{testplansdir}/Zicboz.adoc[]

include::norm/Zicboz_norm_rules.adoc[]


==== Zicbom Cache Block Flush/Invalidate

Zicbom runs the same tests as Zicboz.  `cbo.flush` and `cbo.clean` should have no architecturally visible effect.  `cbo.inval` could discard the cache block, leaving the initial value of 0s.  This may be difficult to match against a reference model, so perhaps this test needs to accept both the original data and 0s as correct answers.  TODO - adjust if any machines need this.

include::{testplansdir}/Zicbom.adoc[]

include::norm/Zicbom_norm_rules.adoc[]

==== Zicbop Cache Block Prefetch

The Zicbop prefetch instructions are hints that do not have to do anything.  They are tested by being executed with a variety of rs1 values and offsets to ensure that they are decoded and executed without trapping, but no specific behavior is required or checked except that the 65 words written to scratch are still read back correctly.

include::{testplansdir}/Zicbop.adoc[]

include::norm/Zicbop_norm_rules.adoc[]

==== Za64rs Max 64B Reservation Set

[[t-Za64rs-coverpoints]]
.Za64rs Coverpoints
//,===
//include::{testplansdir}/Za64rs.adoc[]
//,===

==== Zic64bZicboz 64B Cache Block Zero

[[t-Zic64bZicboz-coverpoints]]
.Zic64bZicboz Instruction Coverpoints
//,===
//include::{testplansdir}/Zic64bZicboz.adoc[]
//,===
