///////////////////////////////////////////
// PMPZca/PMPZca_cret_napot-00.S
//
// This file is autogenerated by testgen.
// Do not modify directly.
//
// Copyright (C) 2025 Harvey Mudd College & Oklahoma State University, UET Lahore, Habib University
// SPDX-License-Identifier: Apache-2.0
///////////////////////////////////////////

##### START_TEST_CONFIG #####
# REQUIRED_EXTENSIONS: ['I', 'Zca', 'Sm']
# params:
#   NUM_PMP_ENTRIES: '>0'
#   PMP_NAPOT_SUPPORTED: true
# MARCH: rv${XLEN}i_zicsr_zifencei_zca
##### END_TEST_CONFIG #####

// Define the size of the signature region
#define SIGUPD_COUNT 14

// Extra defines (if needed) to enable trap handlers, fpu, vector, etc.
#define BOOT_TO_MMODE

// Include framework macros
#include "riscv_arch_test.h"

// Set up test environment and declare main entry point
RVTEST_BEGIN



/////////////////////////////////
// cp_cret_napot
//   Checking that 16-bit fetches adjacent to a NAPOT PMP boundary succeed.
//   Set up a standard NAPOT PMP region with L=1, XWR = 111. Placing four
//   c.ret = c.jr ra statements just below, at bottom, at top, and just
//   above PMP region, half of which are on 16-bit boundaries.
//   Attempt jalr to each c.ret.
/////////////////////////////////

// Clear every pmpcfg and pmpaddr CSR
  .set pmpcfgi, CSR_PMPCFG0
  .rept UDB_NUM_PMP_ENTRIES / (UDB_MXLEN / 8)
  csrw pmpcfgi, x0
  .set pmpcfgi, pmpcfgi + (UDB_MXLEN / 32)
  .endr
  .set pmpaddri, CSR_PMPADDR0
  .rept UDB_NUM_PMP_ENTRIES
  csrw pmpaddri, x0
  .set pmpaddri, pmpaddri + 1
  .endr

#define PMPCFG (((PMP_L | PMP_R | PMP_W | PMP_X | PMP_NAPOT) & 0xFF) << PMP_CFG_SHIFT(0))
#define REGIONSTART TEST_FOR_EXECUTION_1
#if UDB_PMP_GRANULARITY != 2
#define PMP_MASK        ~((1 << (UDB_PMP_GRANULARITY - 3)) - 1)
#define PMP_REGION_SIZE ((1 << (UDB_PMP_GRANULARITY - 3)) - 1)
#else
#define PMP_MASK        ~0
#define PMP_REGION_SIZE 0
#endif

  RVTEST_PMP_SET_BACKGROUND x4

// PMP configuration: NAPOT region with L = 1 and XWR = 111, c.ret just below, at the bottom, at the top and just above it
  LA(x5, REGIONSTART)
  srl x5, x5, PMP_SHIFT
  LI(x6, PMP_MASK)
  and x5, x5, x6
  LI(x6, PMP_REGION_SIZE)
  or x5, x5, x6
  csrw pmpaddr0, x5
  LI(x4, PMPCFG)
  csrw pmpcfg0, x4
  RVTEST_SFENCE_VMA_IF_SUPPORTED

  LA(x15, TEST_FOR_EXECUTION_0)
PMPZca_cp_cret_napot_boundary0_1_c_jalr:
  c.jalr x15
  # Check if x1 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x1, PMPZca_cp_cret_napot_boundary0_1_c_jalr, PMPZca_cp_cret_napot_boundary0_1_c_jalr_str)

  LA(x15, TEST_FOR_EXECUTION_1)
PMPZca_cp_cret_napot_boundary1_1_c_jalr:
  c.jalr x15
  # Check if x1 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x1, PMPZca_cp_cret_napot_boundary1_1_c_jalr, PMPZca_cp_cret_napot_boundary1_1_c_jalr_str)

  LA(x15, TEST_FOR_EXECUTION_2)
PMPZca_cp_cret_napot_boundary2_1_c_jalr:
  c.jalr x15
  # Check if x1 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x1, PMPZca_cp_cret_napot_boundary2_1_c_jalr, PMPZca_cp_cret_napot_boundary2_1_c_jalr_str)

  LA(x15, TEST_FOR_EXECUTION_3)
PMPZca_cp_cret_napot_boundary3_1_c_jalr:
  c.jalr x15
  # Check if x1 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x1, PMPZca_cp_cret_napot_boundary3_1_c_jalr, PMPZca_cp_cret_napot_boundary3_1_c_jalr_str)

// Instantiate trap handlers and other epilog code, call RVMODEL_HALT at end of test
RVTEST_CODE_END

// Create scratch data sections and label for test data
RVTEST_DATA_BEGIN

// Test data

#if __riscv_xlen == 32
  .p2align 11
#else
  .p2align 10
#endif
  .skip 0x806
TEST_FOR_EXECUTION_0:
  ret
TEST_FOR_EXECUTION_1:
  ret
  .rept ((PMP_NAPOT_REGION_BYTES - 4) / 2)
  c.nop
  .endr
TEST_FOR_EXECUTION_2:
  ret
TEST_FOR_EXECUTION_3:
  ret
  .option push
  .option norvc
RETURN_INSTRUCTION:
  nop
  nop
  jr ra
  .option pop

// Testcase strings
PMPZca_cp_cret_napot_boundary0_1_c_jalr_str: .string "\"test: 1; cg: PMPZca; cp: cp_cret_napot; bin: boundary0_1_c.jalr\""
PMPZca_cp_cret_napot_boundary1_1_c_jalr_str: .string "\"test: 2; cg: PMPZca; cp: cp_cret_napot; bin: boundary1_1_c.jalr\""
PMPZca_cp_cret_napot_boundary2_1_c_jalr_str: .string "\"test: 3; cg: PMPZca; cp: cp_cret_napot; bin: boundary2_1_c.jalr\""
PMPZca_cp_cret_napot_boundary3_1_c_jalr_str: .string "\"test: 4; cg: PMPZca; cp: cp_cret_napot; bin: boundary3_1_c.jalr\""

// End of test data section and model specific data
RVTEST_DATA_END

// Signature region for self-checking
RVTEST_SIG_SETUP
